include ../blocks/meta

doctype html
html(lang="ru-RU")
	head
		title #{title}
		meta(charset="utf-8")
		meta(content="width=device-width, initial-scale=1" name="viewport")
		meta(content="ie=edge" http-equiv="x-ua-compatible")

		link(rel="shortcut icon" href="/assets/img/favicon.ico")

		link(rel="stylesheet" href=`/assets/css/foundation${suffix}.css${version}`)
		link(rel="stylesheet" href=`/assets/css/app${suffix}.css${version}`)

		script(src=`/assets/js/foundation${suffix}.js${version}` defer)
		script(src=`/assets/js/admin${suffix}.js${version}` defer)

		<!--[if lt IE 9]>
		script(src="http://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js")
		script(src="http://cdnjs.cloudflare.com/ajax/libs/respond.js/1.4.2/respond.min.js")
		<![endif]-->

	body.bg_forest-full
		.wrapper.admin-bg
			
			header.admin-header
				h1.admin__logo Панель администрирования
				a.admin__toSite(href="/") Вернуться на сайт

			main.admin
				ul.tabs
					each item in ["Навыки", "Блог", "Работы", "Отзывы"]
						li.tabs__li= item

				.tabs__content
					div.adm-content
						include ../blocks/admin/skills
					div.adm-content
						include ../blocks/admin/blog
					div.adm-content
						include ../blocks/admin/works
					div.adm-content
						include ../blocks/admin/speak